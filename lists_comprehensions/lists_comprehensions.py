# Get squares of a list of numbers
random_list = [1, 2, 3, 4, 5, 6, 7]
squares = [n * 2 for n in random_list]
print(squares)

# Get odd numbers from a list
odd_numbers = [n for n in random_list if n % 2 != 0]
print(odd_numbers)
